# Installation
1. Run 'npm install'
2. Copy config.sample.js to config.js
3. Modify to add in your keys

# Running
Run 'node index.js'. You can swap in forever or pm2 or whatever you like for Node in order to create a long running service.